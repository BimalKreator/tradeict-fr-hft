export default function OrdersPage() {
  return (
    <div className="w-full max-w-[100vw] overflow-x-hidden px-4">
      <h2 className="text-lg font-semibold text-[var(--foreground)]">Orders</h2>
      <p className="mt-2 text-sm text-[var(--muted)]">Active and past orders will appear here.</p>
    </div>
  );
}
